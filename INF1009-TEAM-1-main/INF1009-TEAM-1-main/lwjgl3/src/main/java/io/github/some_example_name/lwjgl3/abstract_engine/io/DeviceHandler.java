package io.github.some_example_name.lwjgl3.abstract_engine.io;

// Keyboard and mouse interface
public interface DeviceHandler {
    // Runs every frame to update input state
    void handleInput();
}
